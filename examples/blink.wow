# blink.wow — blink the built-in LED on pin 13
# Flash with: wow build blink.wow --target arduino

LED = 13

banao shuru() {
    pin_set(LED, output)
}

banao chalao() {
    pin_likho(LED, on)
    intezar(1000)
    pin_likho(LED, off)
    intezar(1000)
}
