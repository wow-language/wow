# ginti.wow — Serial monitor par ginti (count up on the serial monitor)
# Flash with:  wow build ginti.wow --target arduino

counter = 0

banao chalao() {
    counter = counter + 1
    likho "Ginti: {counter}"
    intezar(500)
}
